---
"@moonshot-ai/kimi-code-sdk": minor
"@moonshot-ai/kimi-code": minor
---

Add ChatGPT OAuth authentication for OpenAI models in the CLI and in-process harness. Select OpenAI ChatGPT from `/login` to authenticate before choosing a model, or set `openai.authentication` to `chatgpt` and call `harness.auth.loginOpenAI`.
