---
"@moonshot-ai/kimi-code": minor
---

Refresh the TUI with a Claude Code-inspired visual style: a new built-in `claude` theme (now the default), simplified editor borders, unified `❯` selection pointers, and restyled tool call and thinking blocks. Pick another theme with /theme or the `theme` option in tui.toml.
