---
"@moonshot-ai/kimi-code": patch
---

Fail immediately with a clear error when a provider API key is missing instead of burning through retries, and stream per-step token usage so the status bar can show live token counts.
