---
"@moonshot-ai/kimi-code": minor
---

Add a `cache` slot to the footer status line showing the session's prompt-cache hit rate (the share of input tokens served from the provider cache). Reorder or remove it via `[status_line].items` in `tui.toml`.
