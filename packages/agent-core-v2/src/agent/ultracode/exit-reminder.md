## Ultracode Mode Ended

Ultracode mode has ended. You are no longer required to follow the Ultracode workflow.

The user's next request is likely to be a regular request. If it still benefits from subagents you may call the `Agent` or `AgentSwarm` tools, but decide from the new request itself rather than the ended Ultracode workflow.
