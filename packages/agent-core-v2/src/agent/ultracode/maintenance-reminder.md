## Ultracode Mode Still On

Ultracode is still on. Keep the exhaustive-quality bar, and for substantive work keep authoring and running `Workflow` scripts (or `Agent` / `AgentSwarm` subagents) rather than doing the work inline. Use `budget.remaining()` to decide whether to keep fanning out or wrap up and consolidate.
